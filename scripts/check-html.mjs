import fs from 'node:fs';import vm from 'node:vm';const h=fs.readFileSync('public/index.html','utf8');const blocks=[...h.matchAll(/<script>([\\s\\S]*?)<\\/script>/gi)].map(x=>x[1]);if(!blocks.length)throw new Error('NO_INLINE_SCRIPT');for(const [i,s] of blocks.entries())new vm.Script(s,{filename:'public/index.html#script-'+i});if((h.match(/<\\/html>/gi)||[]).length!==1)throw new Error('HTML_DOCUMENT_DUPLICATED');console.log(JSON.stringify({event:'HTML_SCRIPT_GATE_PASS',scripts:blocks.length}));
