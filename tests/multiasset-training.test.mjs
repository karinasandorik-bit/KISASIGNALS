import test from 'node:test';import assert from 'node:assert/strict';import {trainWalkForward} from '../scripts/train-multiasset.mjs';
test('multiasset trainer refuses insufficient history',()=>assert.equal(trainWalkForward([]).status,'INSUFFICIENT_DATA'));
test('walk-forward split is chronological and frozen shadow',()=>{const rows=Array.from({length:260},(_,i)=>({observedAt:new Date(1700000000000+i*3600000).toISOString(),r1Bps:i%7,r4Bps:i%11-5,rvBps:60+i%20,range6hBps:100+i%30,liquidityScore:80,fundingRate:.0001,deltaOiPct:(i%9-4)/10,basisBps:i%5,orderFlow:(i%7-3)/4,depthImbalance:(i%5-2)/3,futureReturnBps:i%3===0?60:i%3===1?-60:0}));const r=trainWalkForward(rows);assert.equal(r.status,'TRAINED');assert.equal(r.artifact.status,'FROZEN_SHADOW');assert.equal(r.artifact.walkForward.oosN,52);assert.ok(Number.isFinite(r.artifact.walkForward.oosBrier))});
