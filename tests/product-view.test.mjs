import test from 'node:test';import assert from 'node:assert/strict';import {signalProductView,evidenceStats} from '../src/product-view.mjs';
test('product view exposes evidence-first signal card',()=>{const x=signalProductView({predictionId:'p1',symbol:'BTCUSDT',ts:'2026-09-24T00:00:00Z',decision:{action:'LONG'},plan:{entry:100,stopLoss:98,takeProfit:104,rr:2,evBps:12},forecast:{pUp:.62,pDown:.23,pRange:.15,LONG:{mfe10:20,mfe50:90,mfe90:180,mae10:10,mae50:35,mae90:80}},model:{calibration_status:'VERIFIED',uncertainty:.2,ood:.1},consensus:{verified:true,tradeAuthority:true,sourceCount:3,sources:['a','b','c'],markSpreadBps:1.2}});assert.equal(x.action,'LONG');assert.equal(x.risk.tp1,104);assert.equal(x.trajectory.probabilities.up,.62);assert.equal(x.trajectory.mfeBps.q90,180);assert.equal(x.consensus.sourceCount,3);assert.equal(x.evidence.mode,'PROSPECTIVE_ONLY');assert.ok(x.prospectiveScore>=0&&x.prospectiveScore<=100)});
test('statistics forbid unsupported accuracy claim',()=>{const s=evidenceStats();assert.equal(s.evidenceMode,'PROSPECTIVE_ONLY');assert.match(s.accuracyClaim,/NOT_AUTHORIZED/)});
