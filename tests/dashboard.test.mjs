import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
test('product terminal exposes mobile evidence-first contract',()=>{const h=fs.readFileSync('public/index.html','utf8'),s=fs.readFileSync('src/dashboard.mjs','utf8');for(const x of ['Signals','Open Entries','Market','Performance','Prospective Score','MFE50','MAE90','PROSPECTIVE','EVIDENCE LAYER','SIGNAL DETAIL','Live vs entry','Venue disagreement','Full Evidence Audit','Entry Zone'])assert.match(h,new RegExp(x));assert.match(h,/data-p="signals"/);assert.match(h,/class="bottom"/);assert.match(h,/s\.product/);assert.match(h,/pr\.signals/);assert.match(h,/pr\.openEntries/);assert.match(s,/\/api\/state/);assert.match(s,/text\/event-stream/);assert.match(s,/runLive/);assert.match(s,/settleEligible/);assert.match(s,/scanUniverse/);assert.match(s,/buildDecisionIntelligence/);});
