import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
test('signal terminal exposes live prospective contract',()=>{const h=fs.readFileSync('public/index.html','utf8');const s=fs.readFileSync('src/dashboard.mjs','utf8');assert.match(h,/Decision now/);assert.match(h,/Trajectory probability/);assert.match(h,/Frozen execution plan/);assert.match(h,/Prospective trials \/ settlements/);assert.match(h,/MFE50/);assert.match(h,/PENDING 4H/);assert.match(h,/chartTip/);assert.match(h,/predictionAudit/);assert.match(h,/Asset Intelligence Matrix/);assert.match(h,/Market context/);assert.match(s,/\/api\/state/);assert.match(s,/text\/event-stream/);assert.match(s,/runLive/);assert.match(s,/settleEligible/);assert.match(s,/scanUniverse/);assert.match(s,/marketContext/);assert.match(s,/captureExternalEvidence/);assert.match(s,/externalEvidence/)});
