import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
test('signal terminal exposes live prospective contract',()=>{const h=fs.readFileSync('public/index.html','utf8');const s=fs.readFileSync('src/dashboard.mjs','utf8');assert.match(h,/Current signal/);assert.match(h,/Trajectory probability/);assert.match(h,/Trade plan/);assert.match(h,/Prospective feed/);assert.match(h,/MFE\/MAE distributions/);assert.match(s,/\/api\/state/);assert.match(s,/text\/event-stream/);assert.match(s,/runLive/)});
